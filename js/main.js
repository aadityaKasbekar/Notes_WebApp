// Importing the `App` class from the "App.js" file.
import App from "./App.js";

// Getting a reference to the HTML element with the id "app".
const root = document.getElementById("app");

// Creating a new instance of the `App` class, passing the HTML element as a parameter.
const app = new App(root);